To run the fruit unit test for the particles:
(run inside the jorek directory)

./util/fruit.sh  particles/tests

with in mod_settings:
n_tor=1

in mod_model_settings:
with_TiTe = .false.

(i.e. use a model with a single temperature)
